# Encouragement Date Invitation

Game-style interactive invitation.

## GitHub Pages

Upload `index.html` to the root of a GitHub repository, then enable GitHub Pages from:

**Settings → Pages → Deploy from a branch → your branch → /(root) → Save**

The Player 2 photo is embedded directly in `index.html`, so no separate image file is required.
